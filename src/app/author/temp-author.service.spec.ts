import { TestBed, inject } from '@angular/core/testing';

import { TempAuthorService } from './temp-author.service';

/*describe('TempAuthorService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [TempAuthorService]
    });
  });

  it('should be created', inject([TempAuthorService], (service: TempAuthorService) => {
    expect(service).toBeTruthy();
  }));
});*/
